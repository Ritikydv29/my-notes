void main()
{
  var map={
    "ritk":1,"jod":4.7,"raju":"project"
  };
  print(map);
  
  // key should always be in string but value can be any data type
  final n=50; //or final datatype n;
  const name= "Raman";// cannot reassign const and has to assign value on declaring
  // cannot reassign final value 
  // const list cannot modify e.g i.e cannot add at last
  // final list can modify
  print(n);
}